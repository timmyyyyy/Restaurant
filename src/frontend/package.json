{
  "name": "restaurant-frontends",
  "version": "1.0.0",
  "private": true,
  "description": "Restaurant micro-frontends monorepo",
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:orders": "npm run dev --workspace=orders-app",
    "dev:restaurant": "npm run dev --workspace=restaurant-app",
    "dev:delivery": "npm run dev --workspace=delivery-app",
    "build": "npm run build --workspaces --if-present",
    "build:orders": "npm run build --workspace=orders-app",
    "build:restaurant": "npm run build --workspace=restaurant-app",
    "build:delivery": "npm run build --workspace=delivery-app",
    "lint": "npm run lint --workspaces --if-present"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
